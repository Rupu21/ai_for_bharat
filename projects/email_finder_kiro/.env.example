# ============================================================
# Email Insights Dashboard - Environment Configuration
# ============================================================
# Copy this file to .env and fill in your actual values
# All variables marked as REQUIRED must be set for the application to run

# ============================================================
# Google OAuth Configuration (REQUIRED)
# ============================================================
# Get these from Google Cloud Console:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Gmail API
# 4. Create OAuth 2.0 credentials
# 5. Add authorized redirect URI: http://localhost:8000/auth/callback

GOOGLE_CLIENT_ID="your_google_client_id_here"
GOOGLE_CLIENT_SECRET="your_google_client_secret_here"
GOOGLE_REDIRECT_URI="http://localhost:8000/auth/callback"

# ============================================================
# AWS Bedrock Configuration (REQUIRED)
# ============================================================
# Get these from AWS IAM Console:
# 1. Create IAM user with Bedrock access
# 2. Generate access keys
# 3. Ensure you have access to Claude 3.7 Sonnet model
# 4. Model ID: us.anthropic.claude-3-7-sonnet-20250219-v1:0
#
# Note: AWS_SESSION_TOKEN is only required if using temporary credentials
# (e.g., from AWS STS, IAM roles, or AWS Academy/Learner Lab)

AWS_ACCESS_KEY_ID="your_aws_access_key_id_here"
AWS_SECRET_ACCESS_KEY="your_aws_secret_access_key_here"
AWS_SESSION_TOKEN="your_aws_session_token_here_if_using_temporary_credentials"
AWS_REGION=us-east-1

# ============================================================
# Application Configuration (REQUIRED)
# ============================================================
# Generate a random secret key for session management
# Example: python -c "import secrets; print(secrets.token_hex(32))"

SESSION_SECRET_KEY=your_random_secret_key_here

# ============================================================
# Logging Configuration
# ============================================================
# Path where application logs will be stored
# Directory will be created automatically if it doesn't exist

LOG_FILE_PATH=logs/app.log
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
LOG_DATE_FORMAT=%Y-%m-%d %H:%M:%S

# ============================================================
# Optional Application Settings
# ============================================================
# These have sensible defaults but can be customized

# Enable debug mode (shows detailed error messages)
DEBUG=False

# Host to bind the server to (0.0.0.0 allows external connections)
HOST=0.0.0.0

# Port to run the server on
PORT=8000
